/*Write a C program to input a number from the user and swap the first and
last digits of the given number.*/
#include<stdio.h>
void main()
{
    int n,fd,ld;
    printf("Enter a 4 digit number : ");
    scanf("%d",&n);
    fd=1000;
    ld=n%10;
}